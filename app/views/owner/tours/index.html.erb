<div class="container">
  <h1>Your Tours</h1>
  <% if @tours.length == 0 %>
    <h2>No tours added yet</h2>
  <% else %>
    <% @tours.each do |tour| %>
      <%= render "tours/tour", tour: tour %>
    <% end %>
  <% end %>
</div>
